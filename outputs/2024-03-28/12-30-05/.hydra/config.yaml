artifact_paths:
  cleaning:
    bool: true
    input_path: ${hydra:runtime.cwd}/data/raw/master_pickle.pkl
    output_path: ${hydra:runtime.cwd}/data/preprocessed/processed_data.pkl
  trained_models:
    output_dir_path: ${hydra:runtime.cwd}/trained_models
cleaning:
  bool: true
  output_path: ${hydra:runtime.cwd}/data/preprocessed/rabi_v1.0
  input_path: ${hydra:runtime.cwd}/data/preprocessed/rabi_v1.0
